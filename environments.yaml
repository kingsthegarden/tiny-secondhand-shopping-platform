name: secure_coding
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - flask>=3.0
    - flask-socketio>=5.3
    - flask-sqlalchemy>=3.1
    - flask-wtf>=1.2
    - simple-websocket>=1.0
    - pytest>=8.0
